# vpm packet manager for vomp

if you cant read code:
1. Create file vpm
2. Paste code then paste the contents from the code file into vpm
3. run this command: sudo chmod +x ./vpm
using:
1. Set a mirror (vpm mirror <global> or vpm)
2. Install pkg (vpm install <pkg>)

